import { Outlet, NavLink } from 'react-router-dom';

export default function Layout() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', minHeight: '100vh' }}>
      <header style={{ padding: '12px 24px', borderBottom: '1px solid #e2e8f0',
                       display: 'flex', alignItems: 'center', gap: 24 }}>
        <span style={{ fontWeight: 700, fontSize: 18 }}>{{ title .Domain }}</span>
        <nav style={{ display: 'flex', gap: 16 }}>
          <NavLink to="/" end style={({ isActive }) => ({ color: isActive ? '#6366f1' : '#64748b' })}>
            Dashboard
          </NavLink>
          {/* APPDARTA-EXTEND: approval-module */}
          {/* Add navigation links for your screens here */}
        </nav>
      </header>

      <main style={{ flex: 1, padding: '24px' }}>
        <Outlet />
      </main>

      <footer style={{ padding: '12px 24px', borderTop: '1px solid #e2e8f0',
                       fontSize: 12, color: '#94a3b8', textAlign: 'center' }}>
        {{ title .Domain }} · {{ .Domain }}
      </footer>
    </div>
  );
}
